# ---------------------------------------------------------------------------
# DELIBERATELY VULNERABLE DEPENDENCY SET -- DO NOT MERGE, DO NOT DEPLOY.
#
# These versions are pinned to known-CVE releases on purpose, as test material
# for Checkmarx SCA triage-and-remediation. They are all legitimate packages
# from PyPI -- nothing here is malicious, just old and unpatched.
#
# The clean, supported dependency set lives on `main`.
# ---------------------------------------------------------------------------

# Core framework, downgraded from Flask 3.1.2 / Werkzeug 3.1.3 / Jinja2 3.1.6
Flask==0.12.5
Werkzeug==0.11.15
Jinja2==2.10
MarkupSafe==0.23

# Transitive/adjacent packages with well-known advisories
PyYAML==5.1
requests==2.19.1
urllib3==1.24.1
cryptography==2.3
